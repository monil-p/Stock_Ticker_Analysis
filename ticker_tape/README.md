# ticker_tape (dbt project)

The dbt project for Ticker Tape. Full documentation, setup instructions and
design notes are in the [repository README](../README.md).

Run dbt commands from this directory, or pass `--project-dir ticker_tape`
from the repo root.

```bash
dbt deps
dbt debug
dbt build
```
